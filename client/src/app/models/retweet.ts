export interface retweet {
    screen_name: string,
    user_id: string,
    range_max: number,
    range_min: number,
    count_max: number,
    status: number,
    status_now: number,
    maxed: boolean
}